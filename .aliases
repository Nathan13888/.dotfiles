#########################
#         ALIAS         #
#########################

eval $(thefuck --alias)
alias f="fuck"

### CLI & APPLICATIONS
alias s="sudo"
alias s!="sudo !!"
alias sudo='sudo ' # for using other alias when using sudo
function reload {
  if [[ $SHELL == */tmux ]] ; then
    exec /usr/bin/zsh
  else
    exec $SHELL # reload whatever is $SHELL
  fi
}
alias ar="paru"
alias open="xdg-open"
#alias scr="screen"
alias tm="tmux"
alias tma="tmux attach"
alias tmr="tmux source-file ~/.tmux.conf"
alias tmks="tmux kill-server"
alias sv='sudo v'
alias nv="nvim"
if [[ $ISSERVER == YES ]] ; then
  alias v=$(which vim)
else
  alias v=$(which nvim)
fi
alias vi="vim"
alias t="todo.sh"
alias g="git"
if [[ $ISSERVER != YES ]] ; then
  alias git="hub"
fi
alias n="npm"
alias py="python"
alias vr='v -r'
alias vm="v -M"
alias l="less"
alias m="more"
alias su="sudo su -"
alias c="cd"
alias clear="clear -x"
alias e="exit"
### WIFI
alias ifa="/sbin/ifconfig -a"
alias ipshow="ip link show"
alias nmds="nmcli device status"
alias nmcs="nmcli connection show"
alias mc="sudo macchanger"
alias mch="macchanger --help"
alias mcl="macchanger -l"
alias mcs="macchanger -s"
alias mcr="mc -r"
alias mce="mc -e"
alias mca="mc -a"
alias mcA="mc -A"
alias mcp="mc -p"
alias airm="sudo airmon-ng" # https://github.com/aircrack-ng/rtl8812au
alias airmck="airm check kill"
alias ipls="sudo ip link set" # reference below from https://stackoverflow.com/questions/55127741/how-do-you-pass-arguments-to-custom-zsh-functions
function iplsd {
  readonly face=${1:?"The interface must be specified"}
  ipls $1 down
  echo "Put interface $1 down"
}
function iplsu {
  readonly face=${1:?"The interface must be specified"}
  ipls $1 up
  echo "Put interface $1 up"
}
function remac {
  readonly face=${1:?"The interface must be specified"}
  iplsd $1
  mcA $1
  iplsu $1
}
function unmac {
  readonly face=${1:?"The interface must be specified"}
  iplsd $1
  mcp $1
  iplsu $1
}
### SYSTEM
alias mount="sudo mount"
alias ipt="sudo iptables"
alias resetlock="faillock --reset --user $USER"
alias free="free -hm"
#alias top="htop" # HTOP is better than TOP
alias xc="xclip"
alias pg="pgrep" # find process id, `$ pidof` is another viable command alternative
function findexe() {
  readonly pid=${1:?"The PID of the process must be specified"}
  readlink -f /proc/$1/exe
}
### APPLICATIONS
alias code="code-insiders"
alias bdctl="betterdiscordctl"
alias chrome="google-chrome"
alias weather="xdg-open https://www.accuweather.com/en/ca/mississauga/l5b/hourly-weather-forecast/55071"
### FILE INFO
alias disks='df -h | grep sd \
    | sed -e "s_/dev/sda[1-9]_\x1b[34m&\x1b[0m_" \
    | sed -e "s_/dev/sd[b-z][1-9]_\x1b[33m&\x1b[0m_" \
    | sed -e "s_[,0-9]*[MG]_\x1b[36m&\x1b[0m_" \
    | sed -e "s_[0-9]*%_\x1b[32m&\x1b[0m_" \
    | sed -e "s_9[0-9]%_\x1b[31m&\x1b[0m_" \
    | sed -e "s_/mnt/[-_A-Za-z0-9]*_\x1b[34;1m&\x1b[0m_"' # list all disks
# SSD Benchmark
function fiotest {
    testFile="/mnt/fs/fiotest.tmp"
    sudo fio --loops=5 --size=1000m --filename=$testFile --stonewall --ioengine=libaio --direct=1 \
      --name=Seqread --bs=1m --rw=read \
      --name=Seqwrite --bs=1m --rw=write \
      --name=512Kread --bs=512k --rw=randread \
      --name=512Kwrite --bs=512k --rw=randwrite \
      --name=4kQD32read --bs=4k --iodepth=32 --rw=randread \
      --name=4kQD32write --bs=4k --iodepth=32 --rw=randwrite
    sudo rm -ivf $testFile
}

alias fd='find . -type d -name' # find directory
alias ff='find . -type f -name' # find file
alias du="du -h"
alias df="df -h"
alias du1="du -d 1" # view the size of all directories of depth 1
alias dutop="du -hsx -- * | sort -rh | head -10" # list top 10 largest files/folders in directory
alias cf="count-files"
alias count-files="ls | wc -l"
alias cfr="count-files-recursive"
alias count-files-recursive="ls -R | wc -l"
#alias ducks="du -cks -- * | sort -rn | head"
##### LS
if command -v exa &> /dev/null; then
    alias ls="exa"
fi
alias la='ls -alhF --color=always --group-directories-first'
alias lag="la | grep --color=always ''"
alias lad="la | grep --color=always '^d'"
alias laf="la | grep --color=always '^-'"
alias lar="ls -R --color=always" # show all directories recursively, useful for small directories
##### LS LESS
alias lal="la | less"
alias lldl="lld | less"
alias llfl="llf | less"

### FILE OPERATIONS
alias remount="sudo mount -av"
alias chm="chmod"
alias chmox='sudo chmod +x'
alias cho="chown"
alias chowm="sudo chown $USER:$USER -v"
alias mkd="mkdir -vp"
alias smkd="sudo mkdir -vp"
alias rs="rsync -avzh --progress"
alias cp='cp -v'
alias mv='mv -v'
function garb {
    # TODO: do the same for all other arguements
    mv $1 ~/ws/gt/garb/
}
alias ga="garb"
alias rm='rm -v'
alias rmf="rm -f" # removes all prompts
alias rmd='rmdir -v'
alias tra="trash -v"
### CD
alias down="cd ~/Downloads"
alias docs="cd ~/Documents"
alias pics="cd ~/Pictures"
alias desk="cd ~/Desktop"
alias ..="cd .."
alias ...="cd ../.."
alias .3='cd ../../..'
alias .4='cd ../../../..'
alias .5='cd ../../../../..'
alias 1='cd -'
alias 2='cd -2'
alias 3='cd -3'
alias 4='cd -4'
alias 5='cd -5'
alias 6='cd -6'
alias 7='cd -7'
alias 8='cd -8'
alias 9='cd -9'

[ -f ~/.aliases.local ] && source ~/.aliases.local
