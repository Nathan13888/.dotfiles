- defaults:
    link:
      relink: true
      create: true
      force: true

- clean: ['~']

- link:
    ~/.local/bin/fetchcord.sh: fetchcord.sh

- create:
    - ~/Desktop/Wallpapers

#- shell:
#  - [git submodule update --init --recursive, Installing submodules]
