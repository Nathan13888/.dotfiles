- defaults:
    link:
      relink: true
      create: true
      force: false

- clean: ['~']

- link:
    ~/.bashrc: .bashrc

#- create:
#    - ~/.dotfiles

- shell:
   - echo "REMEMBER TO `export ISSERVER=YES`"
#  - [git submodule update --init --recursive, Installing submodules]
