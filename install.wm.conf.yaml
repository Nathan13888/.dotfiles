- defaults:
    link:
      relink: true
      create: true
      force: true

- clean: ['~']

- link:
    ~/.config/dunst: dunst
    ~/.config/picom.conf: picom.conf
    ~/.config/polybar: polybar
    ~/.config/rofi: rofi
    ~/.config/i3: i3
    ~/.local/bin/lock.sh: lock.sh
    ~/.local/bin/volume.sh: volume.sh

- create:
    - ~/Desktop/Wallpapers

#- shell:
#  - [git submodule update --init --recursive, Installing submodules]
